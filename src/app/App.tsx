import React from 'react';
import { AppRoutes } from './router/routes';
import '../styles/variables.css';

/**
 * Main App component.
 * @returns {JSX.Element}
 */
const App: React.FC = () => {
    return <AppRoutes />;
};

export default App; 